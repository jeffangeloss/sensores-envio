{
  "name": "sensores-envio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix PaginaSemaforos run dev",
    "dev:host": "npm --prefix PaginaSemaforos run dev -- --host 0.0.0.0",
    "build": "npm --prefix PaginaSemaforos run build",
    "build:dev": "npm --prefix PaginaSemaforos run build:dev",
    "lint": "npm --prefix PaginaSemaforos run lint",
    "preview": "npm --prefix PaginaSemaforos run preview",
    "proxy": "python3 Servidor.py --dir PaginaSemaforos/dist --port 8080"
  }
}
